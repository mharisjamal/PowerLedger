<Window x:Class="PowerLedger.App.WhatsNewWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:PowerLedger.App"
        Title="{Binding Title}" Width="440" SizeToContent="Height"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize" ShowInTaskbar="False"
        Background="{DynamicResource Brush.Panel}" Foreground="{DynamicResource Brush.Ink}"
        UseLayoutRounding="True" SnapsToDevicePixels="True" TextOptions.TextFormattingMode="Display">

    <!-- The button has a row of its own, so however short the screen, it stays in view while the points scroll (the
         same shape as the household prompts). -->
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <ScrollViewer Grid.Row="0" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled" Focusable="False">
            <StackPanel Margin="28,24,28,8">
                <TextBlock Style="{StaticResource Text.Body}" FontSize="16" TextWrapping="Wrap" Text="{Binding Title}" />
                <ItemsControl ItemsSource="{Binding Points}" Margin="0,14,0,0" Focusable="False">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <TextBlock Style="{StaticResource Text.Body}" FontSize="13" TextWrapping="Wrap" Margin="0,0,0,8" Text="{Binding}" />
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
                <Button Style="{StaticResource Link}" Margin="0,6,0,0" HorizontalAlignment="Left" Content="Full notes on GitHub" Command="{Binding OpenFullNotes}" />
            </StackPanel>
        </ScrollViewer>
        <StackPanel Grid.Row="1" Margin="28,4,28,20">
            <Button Style="{StaticResource Quiet}" Content="Close" Command="{Binding Close}" HorizontalAlignment="Right" MinWidth="90" />
        </StackPanel>
    </Grid>
</Window>
