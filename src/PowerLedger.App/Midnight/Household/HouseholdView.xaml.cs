using System.Windows.Controls;

namespace PowerLedger.App.Midnight;

/// <summary>Midnight's Household page (Plan O task M2-3): the explainer or the ledgers, the members as a table and Manage
/// and Sign in as cards, over <see cref="HouseholdViewModel"/>.</summary>
public partial class HouseholdView : UserControl
{
    public HouseholdView() => InitializeComponent();
}
