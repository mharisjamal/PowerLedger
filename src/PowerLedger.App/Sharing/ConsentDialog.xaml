<Window x:Class="PowerLedger.App.ConsentDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:PowerLedger.App"
        Title="Help make PowerLedger better" Width="560" SizeToContent="Height"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize" ShowInTaskbar="False"
        Background="{DynamicResource Brush.Panel}" Foreground="{DynamicResource Brush.Ink}"
        UseLayoutRounding="True" SnapsToDevicePixels="True" TextOptions.TextFormattingMode="Display">
    <Window.Resources>
        <Style x:Key="Prose" TargetType="TextBlock" BasedOn="{StaticResource Text.Body}">
            <Setter Property="TextWrapping" Value="Wrap" />
            <Setter Property="LineHeight" Value="19" />
        </Style>
        <Style x:Key="Purpose" TargetType="TextBlock" BasedOn="{StaticResource Prose}">
            <Setter Property="Margin" Value="0,6,0,0" />
        </Style>
        <Style x:Key="Note" TargetType="TextBlock" BasedOn="{StaticResource Text.Muted}">
            <Setter Property="TextWrapping" Value="Wrap" />
            <Setter Property="Margin" Value="0,10,0,0" />
        </Style>
    </Window.Resources>

    <!-- Owner's round: one screen, two choices. The buttons have a row of their own, so however short the screen, they
         stay in view while the text above scrolls. -->
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <ScrollViewer Grid.Row="0" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled" Focusable="False">
            <StackPanel Margin="28,24,28,8">
                <TextBlock Style="{StaticResource Prose}" FontSize="16" Margin="0,0,0,12" Text="Help make PowerLedger better" />
                <TextBlock Style="{StaticResource Prose}"
                           Text="PowerLedger works fully without sending anything. If you agree, it sends the following to PowerLedger's server once a day." />

                <TextBlock Style="{StaticResource Purpose}" Text="Crash and sensor reports: what went wrong and on which hardware, so it can be fixed." />
                <TextBlock Style="{StaticResource Purpose}" Text="Usage: how PowerLedger is used, so the next version works on what matters." />
                <TextBlock Style="{StaticResource Purpose}" Text="Hardware and power: this PC's parts and its power, minute by minute, so estimates get more accurate for everyone." />
                <TextBlock Style="{StaticResource Purpose}" Text="Share my detailed data: needs Hardware and power turned on." />

                <TextBlock Style="{StaticResource Note}"
                           Text="Let what Hardware and power sends be given or sold, as it is, to researchers, hardware makers and energy companies." />
                <TextBlock Style="{StaticResource Note}"
                           Text="Never sent: your name or your PC's name, serial numbers, files, folders or other programs. Your IP address is used only to find your country, then dropped." />

                <Button Style="{StaticResource Link}" Margin="0,12,0,0" HorizontalAlignment="Left" Content="Privacy policy" Command="{Binding OpenPrivacyPolicy}" />
            </StackPanel>
        </ScrollViewer>
        <StackPanel Grid.Row="1" Margin="28,4,28,20">
            <TextBlock Style="{StaticResource Note}" Margin="0,0,0,0" Foreground="{DynamicResource Brush.Amber}" Text="{Binding Message}"
                       Visibility="{Binding Message, Converter={StaticResource VisibleWhenText}}" />
            <TextBlock Style="{StaticResource Note}" Margin="0,8,0,0" Text="You can change each choice any time in Settings → Privacy." />

            <UniformGrid Rows="1" Columns="2" Margin="0,12,0,0">
                <Button Style="{StaticResource Quiet}" Content="Allow all" Command="{Binding AllowAll}" Margin="0,0,6,0" HorizontalContentAlignment="Center" />
                <Button Style="{StaticResource Quiet}" Content="Decline" Command="{Binding Decline}" Margin="6,0,0,0" HorizontalContentAlignment="Center" />
            </UniformGrid>
        </StackPanel>
    </Grid>
</Window>
